# .env.example
# SPARQL Endpoint Configuration
# URL of the SPARQL endpoint to query for registry data
# Default: http://localhost:3030/registry/sparql (Fuseki running in Docker)
SPARQL_ENDPOINT=http://localhost:3030/registry/sparql
